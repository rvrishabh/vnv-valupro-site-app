/** Stack screens must be transparent so GradientBackground shows through. */
export const transparentStackScreenOptions = {
  headerShown: false,
  contentStyle: { backgroundColor: 'transparent' },
} as const;
